## 这个 PR 动了什么

- [ ] 只动了 `dlc/` 或 `resourcepacks/`（自留区改动会被 `ownership-guard` 打回，见 `CONTRIBUTING.md`）
- [ ] 以**新增文件**为主；若覆盖或替换了内置内容，已在下面写清覆盖了什么
- [ ] 已跑：`compileall` / 单测 / `validate_content` / `audit_separation`
- [ ] 已实际玩过一局，内容能出现、机制能生效

## 加了什么

（一句话）

## 怎么验证的

（命令 / 截图 / 存档）
